<UserControl x:Class="MalirTonight.UI.Views.EmployeeFormView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="600">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding IsEditing, ConverterParameter=Edit|Add Employee, FallbackValue=Employee}"
                   FontSize="24" FontWeight="Bold" Margin="0 0 0 24" />

        <StackPanel Grid.Row="1" MaxWidth="500">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" materialDesign:HintAssist.Hint="First Name"
                         Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0 0 8 16" />
                <TextBox Grid.Column="1" materialDesign:HintAssist.Hint="Last Name"
                         Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}"
                         Margin="8 0 0 16" />
            </Grid>

            <TextBox materialDesign:HintAssist.Hint="Phone"
                     Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0 0 0 16" />

            <TextBox materialDesign:HintAssist.Hint="Email"
                     Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0 0 0 16" />

            <TextBox materialDesign:HintAssist.Hint="Address"
                     Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}"
                     TextWrapping="Wrap" AcceptsReturn="True" MinHeight="60"
                     Margin="0 0 0 16" />

            <TextBox materialDesign:HintAssist.Hint="Base Salary"
                     Text="{Binding BaseSalary, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0 0 0 16" />

            <ComboBox materialDesign:HintAssist.Hint="Department"
                      ItemsSource="{Binding Departments}"
                      SelectedValue="{Binding DepartmentId}"
                      SelectedValuePath="Id"
                      DisplayMemberPath="Name"
                      Margin="0 0 0 16" />
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Style="{StaticResource MaterialDesignFlatButton}"
                    Command="{Binding CancelCommand}"
                    Content="Cancel"
                    Margin="0 0 8 0" />
            <Button Style="{StaticResource MaterialDesignRaisedButton}"
                    Command="{Binding SaveCommand}"
                    Content="Save" />
        </StackPanel>
    </Grid>
</UserControl>
