<UserControl x:Class="MalirTonight.UI.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Reports" FontSize="28" FontWeight="Bold" Margin="0 0 16" />

        <Button Grid.Row="1" Style="{StaticResource MaterialDesignRaisedButton}"
                Command="{Binding GenerateCommand}"
                Content="Generate Report"
                Margin="0 0 0 24" />

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <materialDesign:Card Grid.Column="0" Margin="0 0 8 0" Padding="16" UniformCornerRadius="8">
                <StackPanel>
                    <TextBlock Text="Total Employees" FontSize="12" Opacity="0.7" />
                    <TextBlock Text="{Binding TotalEmployees}" FontSize="28" FontWeight="Bold" Margin="0 8 0 0" />
                </StackPanel>
            </materialDesign:Card>
            <materialDesign:Card Grid.Column="1" Margin="8 0" Padding="16" UniformCornerRadius="8">
                <StackPanel>
                    <TextBlock Text="Present Today" FontSize="12" Opacity="0.7" />
                    <TextBlock Text="{Binding PresentToday}" FontSize="28" FontWeight="Bold" Margin="0 8 0 0" />
                </StackPanel>
            </materialDesign:Card>
            <materialDesign:Card Grid.Column="2" Margin="8 0 0 0" Padding="16" UniformCornerRadius="8">
                <StackPanel>
                    <TextBlock Text="Unpaid Salaries" FontSize="12" Opacity="0.7" />
                    <TextBlock Text="{Binding TotalSalary, StringFormat={}{0:C}}" FontSize="28" FontWeight="Bold" Margin="0 8 0 0" />
                </StackPanel>
            </materialDesign:Card>
        </Grid>
    </Grid>
</UserControl>
