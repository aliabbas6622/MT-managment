<UserControl x:Class="MalirTonight.UI.Views.ExpenseListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Expenses" FontSize="28" FontWeight="Bold" Margin="0 0 0 16" />

        <Grid Grid.Row="1" Margin="0 0 0 16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0" materialDesign:HintAssist.Hint="Title"
                     Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0 0 8 0" />
            <TextBox Grid.Column="1" materialDesign:HintAssist.Hint="Amount"
                     Text="{Binding Amount, UpdateSourceTrigger=PropertyChanged}"
                     Margin="8 0 8 0" />
            <ComboBox Grid.Column="2" materialDesign:HintAssist.Hint="Category"
                      SelectedItem="{Binding SelectedCategory}"
                      ItemsSource="{Binding Categories}"
                      Margin="8 0 8 0" />
            <Button Grid.Column="3" Style="{StaticResource MaterialDesignRaisedButton}"
                    Command="{Binding AddCommand}"
                    Content="Add Expense" />
        </Grid>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Expenses}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  materialDesign:DataGridAssist.CellPadding="8">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Date" Binding="{Binding Date, StringFormat={}{0:MMM dd}}" Width="*" />
                <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="2*" />
                <DataGridTextColumn Header="Amount" Binding="{Binding Amount, StringFormat={}{0:C}}" Width="*" />
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
