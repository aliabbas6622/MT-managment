<UserControl x:Class="MalirTonight.UI.Views.AttendanceListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Attendance" FontSize="28" FontWeight="Bold" Margin="0 0 0 16" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0 0 0 16">
            <DatePicker SelectedDate="{Binding SelectedDate}" Width="200" />
            <Button Style="{StaticResource MaterialDesignFlatButton}"
                    Command="{Binding LoadCommand}"
                    Content="Load" Margin="16 0 0 0" />
        </StackPanel>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Attendances}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  materialDesign:DataGridAssist.CellPadding="8">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Employee ID" Binding="{Binding EmployeeId}" Width="*" />
                <DataGridTextColumn Header="Date" Binding="{Binding Date, StringFormat={}{0:MMM dd, yyyy}}" Width="*" />
                <DataGridTextColumn Header="Check In" Binding="{Binding CheckIn}" Width="*" />
                <DataGridTextColumn Header="Check Out" Binding="{Binding CheckOut}" Width="*" />
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="*" />
                <DataGridTextColumn Header="Notes" Binding="{Binding Notes}" Width="2*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
